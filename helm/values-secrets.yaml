# values-secrets.yaml
# DEPRECATED: Hardcoded secrets.
# When Vault is enabled (vault.enabled: true), credentials are dynamically fetched from HashiCorp Vault.
components:
  db:
    rootPassword: "" # Injected via Vault KV secret (secret/data/todo-app/db)

jwt:
  secret: "" # Injected via Vault KV secret (secret/data/todo-app/jwt)

