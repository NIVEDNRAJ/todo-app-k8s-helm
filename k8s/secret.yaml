apiVersion: v1
kind: Secret
metadata:
  name: todo-secrets
  namespace: todo-app
type: Opaque
# Replace placeholders or generate via: kubectl create secret generic todo-secrets --from-literal=mysql-root-password=... -n todo-app
stringData:
  mysql-root-password: "" # Managed by Vault or secret manager
  jwt-secret: "" # Managed by Vault or secret manager
